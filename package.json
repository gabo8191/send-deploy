{
	"name": "send-deploy",
	"version": "1.0.0",
	"description": "button send remote server ssh",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "node --watch index.js"
	},
	"author": "gcastillo",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.21.0",
		"ssh2": "^1.15.0"
	}
}
